OPENAI_API_KEY=sk-...

# Optional -- powers /stats. Without these, /stats just says stats aren't
# configured yet; nothing else in the app depends on them. Auto-populated
# if you connect Upstash Redis via the Vercel dashboard's Marketplace tab.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
